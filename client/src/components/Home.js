import PropertyCard from "./PropertyCard";
import Searchbar from "./Searchbar";

const Home = (props) => {
  return (
    <div>
      <Searchbar />
      <PropertyCard />
    </div>
  );
};

export default Home;
